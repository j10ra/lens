{
  "name": "lens",
  "private": true,
  "version": "0.1.0",
  "description": "LENS — Local-first repo context engine",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev:encore": "cd apps/encore && encore run",
    "build:cli": "pnpm --filter @lens/cli build"
  },
  "dependencies": {
    "encore.dev": "^1.53.7"
  }
}